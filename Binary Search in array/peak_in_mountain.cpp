#include<iostream>
using namespace std;
int main()
{
int arr[6] = {1,2,3,3,3,5};

    int s=0,e=n-1;
    int mid = s + (e-s)/2;

    if(arr[mid < arr[mid + 1]])
    {
        s = mid + 1;
    }
    else{
        e = mid;
    }
    return 0;
}